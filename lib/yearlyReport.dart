import 'package:flutter/material.dart';

void main() => runApp(YearlyReport());

class YearlyReport extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Yearly Report',
      debugShowCheckedModeBanner: false,
      
    );
  }
}
